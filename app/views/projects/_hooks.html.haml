.row-fluid
  .span12
    - plugins.sort.each do |type, plugin|
      .plugin
        %h4= plugin.name.sub(/^Dashbozu::Input/, '')
        %pre
          - url = hook_url(project.api_key, type)
          = link_to url, url
