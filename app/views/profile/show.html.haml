%h3 Profile

%ul.nav.nav-tabs
  %li.active= link_to t(:button_profile), profile_show_path
  %li= link_to t(:button_repositories), projects_from_service_path('github')

%dl.dl-horizontal

- current_user.service_users.each do |user|
  %dl.dl-horizontal
    %dt= t(:label_provider)
    %dd= user.provider
    %dt= t(:label_image)
    %dd= image_tag user.image, width: 48, class: 'img-rounded'
    %dt= t(:label_user_name)
    %dd= user.name
    %dt= t(:label_user_nickname)
    %dd= user.nickname
