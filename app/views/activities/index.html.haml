%ul.breadcrumb
  - unless @project
    %li.active
      = 'Activities'
  - else
    %li
      = link_to 'Projects', projects_path
      %span.divider &gt;
    %li.active
      = @project.name

.activity-list
  = render partial: 'activity', collection: @activities, locals: {target: nil}

= paginate(@activities, window: 2)

