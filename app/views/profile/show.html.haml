%h3 Profile

%ul.nav.nav-tabs
  %li.active= link_to t(:button_profile), profile_show_path
  %li= link_to t(:button_repositories), projects_from_service_path('github')

%dl.dl-horizontal

- current_user.auths.each do |auth|
  %dl.dl-horizontal
    %dt= t(:label_provider)
    %dd= auth.provider
    %dt= t(:label_image)
    %dd= image_tag auth.image, width: 48, class: 'img-rounded'
    %dt= t(:label_user_name)
    %dd= auth.name
    %dt= t(:label_user_nickname)
    %dd= auth.nickname
