- content_for :title do
  Project Hooks

.nav-bar
  .row-fluid
    %h3{style: 'display:inline'} Project Hooks
.row-fluid
  .pull-right= link_to 'Back to project', project_activities_path(@project.api_key)
= render partial: 'hooks', locals: {project: @project, plugins: @plugins }
